# Production (cloud tier, behind nginx). The React app and the API are served
# from the same origin, so the API path is just /api — no host, no CORS.
# Used automatically by `npm run build` (NODE_ENV=production).
REACT_APP_API_URL=/api
